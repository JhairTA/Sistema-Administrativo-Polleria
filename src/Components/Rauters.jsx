import { Router, Switch } from "react-router-dom";
import Navbar from "./Navbar";
import Login from "./Login";

const Rauters = () => {
    return(
        prueba
        // <Router>
        //     <Switch>
        //         <Router exact path="/" component={Login}/>
        //         <Router exact path="/menu" component={Navbar}/>
        //     </Switch>
        // </Router>
    )
}

export default Rauters;